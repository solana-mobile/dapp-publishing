/*
 * Module responsible for validating metadata schemas
 * TODO(jon): Actually, this should go into `core` as a prerequisite to publish
 */

export const validateMetadata = () => {
  // Take a folder's folder / file structure, validate NFT metadata
  // - [ ] Use JSON schema to describe the NFT metadata
};
